

server:
    port: 8080
spring:
    #creiar db na fatec antes de rodar
    datasource:
        url: jdbc:mysql://localhost:3306/testdb
        #username: colocar da fatec
        #password: colcar da fatec
        driver-class-name:
    jpa:
        hibernate:
            ddl-auto: none
        show-sql: true
    mail:
        host: smtp.gmail.com
        port: 587
        #username: email bookshifter
        #password: pegar no notion do Jo�o
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
cors:
    origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}
#A linha abaixo ativa debug no prompt
logging:
    level:
        org:
            springframework:
                security: DEBUG